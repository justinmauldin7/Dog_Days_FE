<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Dog Days</title>
  </head>
  <body>
    <div class="logo">
      <i class="fas fa-paw"></i>
      <h1>Dog Days</h1>
    </div>
    <div class="all_favorites_button">
      <p><%= link_to "My Favorites", favorites_path, class: "button" %></p>
    </div>
    <div class="search">
      <%= form_tag root_path, method: :get do %>
        <%= text_field_tag :search, params[:search], placeholder: "Please enter a dog breed...", class: 'serach_field' %>
        <%= submit_tag "Search", class: "button" %>
      <% end %>
    </div>

    <% if params[:search] %>
      <div class="container">
        <% @dog_images.each do |image| %>
          <div class="search_results">
            <img id="dog_image" src="<%= image %>" alt="<%= @breed %>">
            <h4><%= @breed %></h4>
            <%= link_to "Favorite", favorites_path(breed: @breed, image: image), method: :post, class: 'button' %>
          </div>
        <% end %>
      </div>
    <% end %>
  </body>
</html>
